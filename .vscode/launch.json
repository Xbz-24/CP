{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}", // Use VSCode variables
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false,
            "preLaunchTask": "build C++" // Task name to execute before debugging
        }
    ]
}
